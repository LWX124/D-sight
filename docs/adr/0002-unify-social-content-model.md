# 统一社媒内容模型

D-sight 将公众号、微博、小红书和 B站统一建模为平台发布者、平台内容、用户订阅、金融信源池成员及热榜排名，并把平台特有字段保留为扩展数据，而不再为每个平台复制整套业务表。这样社媒订阅与 AIHot 可以复用同一份内容并降低新增平台的成本；代价是平台字段不能全部依赖独立强类型表约束，需要在数据提供方适配边界完成校验。现有公众号与微博数据必须完整迁入统一模型并完成数量和关键字段对账，旧表在一个版本周期内只读保留，作为迁移回滚边界。
